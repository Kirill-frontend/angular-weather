import { GetTimeFromStrPipe } from './get-time-from-str.pipe';

describe('GetTimeFromStrPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTimeFromStrPipe();
    expect(pipe).toBeTruthy();
  });
});
